'use strict';

boinsoMCCApp.factory('sessionLostInterceptor',
    ['$q', function($q) {

        var sessionRecoverer = {
            responseError: function(response) {
                
            }
        }

    }]
);
